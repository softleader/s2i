name: s2i
version: 0.3.2
usage: build source to image to SoftLeader docker swarm ecosystem
description: s2i is a command line tool to build source to image to SoftLeader docker swarm ecosystem
exec:
  command: $SL_PLUGIN_DIR/s2i
ignoreglobalflags: false
github:
  scopes:
  - read:org
  - user
  - repo
